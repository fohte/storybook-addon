// Synthesized at build time by @storybook/builder-vite's
// storybook-project-annotations-plugin — it ships no `.d.ts` of its own.
declare module 'virtual:/@storybook/builder-vite/project-annotations.js' {
  import type {
    ProjectAnnotations,
    WebRenderer,
  } from 'storybook/internal/types'

  export function getProjectAnnotations(): ProjectAnnotations<WebRenderer>
}
